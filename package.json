{
  "name": "api_gc_esportes",
  "version": "1.0.0",
  "main": "server.ts",
  "license": "MIT",
  "scripts": {
    "dev": "ts-node-dev --exit-child --transpile-only --ignore-watch node_modules src/server.ts",
    "vercel-build": "yarn prisma migrate deploy"
  },
  "dependencies": {
    "@prisma/client": "5.3.1",
    "express": "^4.17.2",
    "prisma": "^5.3.1"
  },
  "devDependencies": {
    "@types/express": "^4.17.13",
    "ts-node-dev": "^1.1.8",
    "typescript": "^5.2.2"
  }
}
